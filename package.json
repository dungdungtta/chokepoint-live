{
  "name": "chokepoint-live",
  "version": "0.1.0",
  "description": "Live Data Feed — Chokepoint Intelligence",
  "type": "module",
  "scripts": {
    "dev": "node scripts/fetch-data.js --local",
    "fetch": "node scripts/fetch-data.js",
    "deploy:worker": "wrangler deploy",
    "worker:secret": "wrangler secret put"
  },
  "devDependencies": {
    "dotenv": "^16.0.0",
    "wrangler": "^4.87.0"
  }
}
